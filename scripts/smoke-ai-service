#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

bun run --cwd "$ROOT/ai-service" build
"$ROOT/scripts/gcx" verify completions --fake --plain

echo "Sidecar smoke test passed."
